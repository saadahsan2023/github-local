const student = {
    name: 'John Doe',
    age: 25,
    grade: 'A',
    subjects: ['Math', 'Science', 'English']

}

const { name, grade, age } = student;

console.log(`Name: ${name} , Grade: ${grade}, Age: ${age}`);
